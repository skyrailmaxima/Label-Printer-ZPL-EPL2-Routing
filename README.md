# Label-Printer-ZPL-EPL2-Routing
Installs a small python script and routes .zpl .epl .epl2 label files to a known/installed printer of your choosing. Also creates a batch application for large sets of labels. Works in a few tested corpo W11 env. where many old fixes no longer work for old printers. 
